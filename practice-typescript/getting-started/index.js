// function practice(a, b) {
//     return a + b;
// }
// practice(10, 20);

// 이렇게 하면 아직 브라우저에서 타입스크립트를 인식하지 못한다.
// 따라서 컴파일을 통해 자바스크립트 파일로 변환을 해줘야한다.
// npm i typescript -g 를 통해 typescript 패키지를 설치해준다.
// tsc index.ts 를 통해 컴파일을 해줄 수 있다.
